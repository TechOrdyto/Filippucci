# Dipendenze per il servizio OCR PaddleOCR
# Installazione: pip install -r requirements.txt
# NOTA: numpy<2 è obbligatorio per PaddleOCR 2.x (cv2 compilato con numpy 1.x)

paddlepaddle>=2.6.0
paddleocr==2.7.3
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
Pillow>=10.0.0
numpy<2
python-multipart>=0.0.9